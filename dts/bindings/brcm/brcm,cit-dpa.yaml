---
title: Broadcom DPA register map
version: 0.1

description: >
    This binding describes the Broadcom DPA registers

properties:
    compatible:
      category: required
      type: string
      description: compatible strings
      constraint: "brcm,cit-dpa"

    reg:
      category: required
      type: int
      description: mmio register space
      generation: define

...
