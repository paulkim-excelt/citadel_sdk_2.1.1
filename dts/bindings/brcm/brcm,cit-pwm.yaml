---
title: Broadcom PWM Controller
version: 0.1

description: >
    This binding describes the Broadcom PWM Nodes

properties:
    compatible:
      category: required
      type: string
      description: compatible strings
      constraint: "brcm,cit-pwm"

    reg:
      category: required
      type: int
      description: mmio register space
      generation: define

...
