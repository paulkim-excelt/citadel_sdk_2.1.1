---
title: Broadcom OTP Controller register map
version: 0.1

description: >
    This binding describes the Broadcom OTP controller registers

properties:
    compatible:
      category: required
      type: string
      description: compatible strings
      constraint: "brcm,cit-otpc"

    reg:
      category: required
      type: int
      description: mmio register space
      generation: define

...
