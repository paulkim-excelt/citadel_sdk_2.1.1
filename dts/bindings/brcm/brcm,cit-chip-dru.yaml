---
title: Broadcom CHIP DRU (Device Resource Unit)
version: 0.1

description: >
    This binding describes the Broadcom CHIP DRU

properties:
    compatible:
      category: required
      type: string
      description: compatible strings
      constraint: "brcm,cit-chip-dru"

    reg:
      category: required
      type: int
      description: mmio register space
      generation: define

...
